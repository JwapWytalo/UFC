/*
Crie um programa que calcula o comprimento de
uma string (não use a função strlen).
*/
#include <stdio.h>

int main()
{
    char str[] = "hello world";
    int i;

    for (i = 0; str[i] != '\0'; i++)
    {
    }

    printf("%d", i);

    return 0;
}